### Description
This package provides functionality for differential expression analysis of read counts from mRNA sequencing data or miRNA expression values
generated by the CAP-miRSEQ 'expression_reports.sh' script. The workflow follows the 'edgeR'-'limma' expression data analysis pipeline providing options
for different approaches, such as "pure" 'edgeR', 'voom' or paired samples. The functions in the package generate text files with differential expression lists,
optionally annotated with information from 'biomart', expression summary plots as well as several QC plots.
The package comes with a wrapper function, "diffExpr", performing all steps of the analysis. Most sub-functions are exported and can be called by the user, as well, if desired.
These functions may be applicable to different kinds of data/input, rely, however, on the conventions set for this package.

### Installation
```
devtools::install_git('git@github.com:vfey/diffr.git')
```

### Prerequisites
Make sure the following packages and libraries are installed (in the given order):
```
#### In the Mac shell ####
## Depending on yur installation, do ONE of the following:
### Homebrew
brew install openssl libgit2

### Macports
sudo port install openssl
sudo port install libgit2

#### In R ####
devtools::install_github('ropensci/git2r')
devtools::install_git('git@gitlab.com:genevia/diffr.git')
```

### NOTE
So far, only the most cryptic arguments are properly documented (due to the lack of time). Running the wrapper may be too complex for
most purposes but by loading the package all sub-functions are exported and can hence be used. I have not tested calling the modelling
functions directly but the plots work on standard R object classes:

##### PCA plots
`prcomp`

##### QC plots
`matrix` or any data object which can be coerced to a matrix, for example a `data.frame`, an `ExpressionSet` or an `EList`.

##### DGE plots
`data.frame` (of annotated comparison results as produced by, e.g., `topTable`)
